# yumao 摄影使用手册

默认生成可独立复制的提示词；明确说“直接出图”才调用图像工具。提示词不默认保存文件。

每套首行使用“01｜角色或主题 · 方案名称”，不加“标题：”或“序号：”。单套也编号，多套依次递增，修订保留对应序号。正文采用自然、准确的中文，写清对象、位置与结构关系，不靠抽象形容词代替实际设计。

## 最简单的输入

给出角色或主题、成年渲染方向、数量和保留要求即可。默认中央连接件，不需要选择侧部布局。四形态请求会保持中央连接设计与同款服装，再改变羽流；材质、动作和摄影按明确开放范围安排。

最小示例：`成年原创角色，同款服装，中央连接，四形态各一套提示词。`

## 四形态与参考

收束是羽片横向依次叠压、上下展开角较小；开羽是外端增大展开角；垂羽是脸侧连续下弯；环覆是沿太阳穴向侧后包覆。定义见[构造](references/form-family.md)。

打开[交互结构图](references/structure-atlas.html)比较正面、斜侧与环覆俯视；不能显示HTML时看[PNG登记](references/feather-form-reference.md)。这是低保真结构说明，不是写真实物效果或已验证生成样图。示意配色、饰品形状和标注不要求照搬。

## 侧源是扩展

明确要求侧源时，使用[旧编号兼容表](references/side-origin-layouts.md)。旧03本身为垂羽，04为开羽，06为主辅环覆；不能把这些固定路径当成任意四形态容器。旧10改作材料探索入口，并说明实际空间路径，元素材质本身不要求侧源或实体底板。已有编号不偷偷换义。

## 参考与一致性

说明附件分别用于身份、服装、动作还是眼羽；给生图工具时也需提供对应图片。Agent看过技能图片不代表生成器收到它。无图时可写完整文字，但不能保证同脸或逐像素同款。

`strict` 保持同一连接、配色、材料与工艺，仅改变指定项；`adaptive` 在已授权换装时重新适配；`free` 探索未锁定项。任何模式都不能覆盖明确服装或形态要求。未指定布局用centered，未指定比例用9:16，像素自动；不默认8K。

## 反馈与验证

提供实际输入与对应成图，指出可见问题，如真实露眼、接缝假眼、中央连接件缺失、羽流跑到背部或鞋子重复。局部修复保持已认可设计，不以换款冒充修复。文字检查与真实出图分别验收，失败图不变成新的正向模板。
